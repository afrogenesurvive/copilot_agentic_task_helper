{
  "name": "frontdesk-operator",
  "version": "0.3.2",
  "private": true,
  "description": "Frontdesk v2 operator dashboard — monitor queues, logs, sessions, licenses, and run MCP tools from a macOS desktop app",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "make:icon": "electron ../scripts/make-icon.mjs",
    "postinstall": "node ../scripts/patch-electron-app-name.mjs",
    "dist:mac": "electron-builder --mac",
    "dist:mac-dir": "electron-builder --mac dir"
  },
  "devDependencies": {
    "electron": "^33.4.11",
    "electron-builder": "^25.0.0"
  },
  "build": {
    "appId": "com.frontdesk.operator",
    "productName": "Frontdesk Operator",
    "icon": "assets/icon.png",
    "mac": {
      "category": "public.app-category.productivity",
      "icon": "assets/icon.icns",
      "target": [
        "dmg",
        "zip"
      ],
      "hardenedRuntime": false
    },
    "files": [
      "src/**/*",
      "assets/**/*"
    ],
    "extraResources": [
      {
        "from": "../scripts",
        "to": "scripts",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "../shared",
        "to": "shared",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "../mcp",
        "to": "mcp",
        "filter": [
          "**/*",
          "!**/node_modules/**"
        ]
      },
      {
        "from": "../webapp",
        "to": "webapp",
        "filter": [
          "**/*",
          "!**/node_modules/**"
        ]
      },
      {
        "from": "../electron/docs",
        "to": "electron/docs",
        "filter": [
          "**/*"
        ]
      }
    ]
  }
}
